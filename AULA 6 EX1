#include <stdio.h>

void conta(int n) {
    int i;

    for(i = 1; i <= n; i++) {
        printf("%d\n", i);
    }
}

int main() {
    int numero;

    printf("Digite um numero: ");
    scanf("%d", &numero);

    conta(numero);

    return 0;
}
